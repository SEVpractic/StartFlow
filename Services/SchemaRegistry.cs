namespace StartFlow.Services;

/// <summary>
/// Реестр версий схемы профиля. Текущая новая структура — первая версия (schemaVersion = 1).
/// </summary>
public static class SchemaRegistry
{
    public const int CurrentSchemaVersion = 1;
}